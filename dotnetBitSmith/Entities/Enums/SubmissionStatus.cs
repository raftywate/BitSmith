namespace dotnetBitSmith.Entities.Enums {
    public enum SubmissionStatus {
        Pending,
        Running,
        Accepted,
        WrongAnswer,
        TimeLimitExceeded,
        MemoryLimitExceeded,
        CompilationError

    }
}